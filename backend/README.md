# NeuroShield AI Backend

FastAPI service with JWT auth, RBAC, audit logging, WebSocket alert streaming, seeded SOC data, and Scikit-learn-ready AI prediction services.

Run:

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```
